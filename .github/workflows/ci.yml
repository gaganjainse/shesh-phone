name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Least privilege by default; jobs that write declare it explicitly.
permissions: read-all

jobs:
  component:
    uses: gaganjainse/shesh-ecosystem/.github/workflows/component-ci.yml@c7abf80eb5ba15338def113201f9f6686b682c66  # first-party reusable pipeline (D1); @main so rewrites never orphan the pin
    with:
      extra-pip: pillow  # TemplateVision (vision_loop) requires pillow
