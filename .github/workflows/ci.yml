name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Least privilege by default; jobs that write declare it explicitly.
permissions: read-all

jobs:
  component:
    uses: gaganjainse/shesh-ecosystem/.github/workflows/component-ci.yml@a392d20f5a07eb14b42a0c6522946276bbf2a798  # first-party reusable pipeline (D1); @main so rewrites never orphan the pin
    with:
      extra-pip: pillow  # TemplateVision (vision_loop) requires pillow
